# Google ↔ Outlook カレンダー同期プログラム仕様書（最終版）

## 1. 概要

本プログラムはGoogle Apps Script（GAS）を用いて、GoogleカレンダーとOutlookカレンダーの予定を双方向同期する。

同期は30分間隔で実行され、JST基準で「当日 00:00:00 から 1か月後 23:59:59.999」までの期間に1日でも重なるイベント（開始〜終了のいずれかが期間に重なるもの）を対象に、差分ベースで作成・更新・削除を行う。

---

## 2. システム構成

### ファイル構成

* code.gs：全体制御・同期処理
* googleCalendarManager.gs：Google操作
* outlookCalendarManager.gs：Outlook操作（ICS + Graph API）

---

## 3. データ保持方針

* 永続的なデータ保存は行わない
* 毎回以下を取得して同期：

  * Googleイベント一覧
  * Outlookイベント一覧
* 処理中のみ一時データ（配列・Map）を使用

---

## 4. イベント識別

### descriptionフォーマット

#### Googleイベント（Outlook由来）

（本文）

outlook_id:<id>
Repeat:<n>

#### Outlookイベント（Google由来）

（本文）

google_id:<id>
Repeat:<n>

#### GoogleのイベントIDに関して

googleのイベントIDに関して、通常のevent.idで取得できるのはcalendarIDのみなので、ちゃんとイベントIDを取得するために以下の様にeventのhtmlLinkを取得してそれをセパレートする形でIDを実装しなさい。
```javascript
function get_event_id() {
  var events = Calendar.Events.list("primary", {
    timeMin: new Date().toISOString(),
    singleEvents: true,
    orderBy: "startTime",
    maxResults: 1
  });
  var link = events.items[0].htmlLink;
  var event_id = link.split("eid=")[1];
  Logger.log([link, event_id]);
}
```

---

## 5. 同期判定

* IDあり → 同期済み
* IDなし → 新規イベント

対応イベントはdescription内IDで特定する

### 同期先検索ルール

* Google → Outlook では、同期先（Outlook）の検索範囲内イベントから `google_id:<GoogleイベントID>` を持つイベントを先に検索する
* Outlook → Google では、同期先（Google）の検索範囲内イベントから `outlook_id:<OutlookイベントID>` を持つイベントを先に検索する
* 同期元イベント側に保存済みの対向イベントID（`outlook_id` / `google_id`）があればそれを優先し、見つからない場合は上記の概要欄ID検索で対応イベントを特定する

### 重複作成防止手順（Google → Outlook）

1. Google と Outlook から先1か月分のイベントを取得する（開始〜終了が1日でもウィンドウに重なるもの）。
2. Google イベント（GE）ごとに、description に `outlook_id:` があれば除外する。
3. Outlook イベントの description から `google_id:` を持つイベントをすべて抽出し、`同期済み Google イベント ID 一覧` として保持する。
4. GE の ID が `同期済み Google イベント ID 一覧` に含まれていれば Outlook の対応イベントを更新し、含まれていなければ Outlook に新規作成する。

### 重複作成防止手順（Outlook → Google）

1. Outlook と Google から先1か月分のイベントを取得する（開始〜終了が1日でもウィンドウに重なるもの）。
2. Outlook イベント（OE）ごとに、description に `google_id:` があれば除外する。
3. Google イベントの description から `outlook_id:` を持つイベントをすべて抽出し、`同期済み Outlook イベント ID 一覧` として保持する。
4. OE の ID が `同期済み Outlook イベント ID 一覧` に含まれていれば Google の対応イベントを更新し、含まれていなければ Google に新規作成する。

### 同期戻し防止

* Google → Outlook 同期では、description に `outlook_id:` が入っているイベントを除外する
* Outlook → Google 同期では、description に `google_id:` が入っているイベントを除外する
* これにより、元カレンダーから同期先へ複製されたイベントが、再度元カレンダーへ戻されるのを防ぐ

---

## 6. 繰り返しイベント

### 方針

* 展開済みイベント（singleEvents: true）を使用

### ICS対応

* RRULE：繰り返し定義
* EXDATE：除外
* RECURRENCE-ID：例外

### 処理

1. 1か月分展開
2. EXDATE除外
3. 例外上書き
4. 日付順にRepeat付与

---

## 7. 同期ルール

### 作成

同期先に対応イベントが見つからない場合のみ新規作成し、description に対応ID（`google_id` / `outlook_id`）を付与する

### 更新

同期先に対応イベントが見つかった場合は新規作成せず更新する

### ID補正

同期元イベントに保持されている対向イベントIDと、同期先検索で見つかったイベントIDが不一致の場合は同期元の description を最新IDで補正する

### 削除

description に対応IDを持つリンク済みイベントについて、対応する同期元イベントが検索範囲内に存在しない場合は同期先イベントを削除する

### 戻し防止

同期先のイベント ID を description に保持しているイベントは、元カレンダーへ戻さない

### 日付・終日変換

* Google 終日イベント（`start.date` / `end.date`）は Outlook の終日イベント（`isAllDay: true`）として同期する
* Outlook 終日イベント（`isAllDay: true`）は Google の終日イベント（`start.date` / `end.date`）として同期する
* 時刻付きイベントは `dateTime` を維持して相互同期する
* Outlook への終日登録時は当日 00:00:00 基準の値に正規化して送信する

### 重複

異なる起源イベントは重複許可

---

## 8. 差分判定

比較項目：

* タイトル
* 開始・終了時刻
* 終日フラグ（isAllDay / date）
* 説明
* 繰り返し番号
* 公開設定
* 空き状態

---

## 9. 公開設定・状態同期

### 公開設定

Google public → Outlook normal
Google private → Outlook private

### 空き状態

Google opaque → Outlook busy
Google transparent → Outlook free

---

## 10. タイムゾーン

* JSTに統一
* ICSのUTCを変換
* API の取得範囲（timeMin/timeMax, startDateTime/endDateTime）は `Asia/Tokyo` のオフセット付き日時で送信する

---

## 11. Map最適化

以下のMapを構築：

* Google

  * byId
  * byOutlookId

* Outlook

  * byId
  * byGoogleId

---

## 12. ログ

### 個別ログ

「イベント名」がGoogle → Outlook に作成されました
「イベント名」がOutlook → Google に更新されました
「イベント名」が削除されました

などと表示し、これらは必ずそれぞれの処理後に表示する

### 集計ログ

=== 同期結果 ===

Google → Outlook
作成: X
更新: X
削除: X

Outlook → Google
作成: X
更新: X
削除: X

---

## 13. 実行スケジュール

* 30分間隔トリガー

---

## 14. Outlook認証

outlookOauth2.gs に実装した認証フローを使い、最初にユーザーが手動で認証コードを取得し、それ以降は refresh_token を使って自動更新して運用する。

### 保存

Script Propertiesに保存：

* CLIENT_ID
* TENANT_ID
* OUTLOOK_CALENDAR_ID
* REFRESH_TOKEN
* ACCESS_TOKEN

### カレンダー指定

* `OUTLOOK_CALENDAR_ID` を設定した場合は、その Outlook カレンダーを同期対象にする
* 未設定の場合は既定の Outlook カレンダーを使う

### フロー

1. 認証URL生成
2. code取得
3. トークン取得
4. 自動更新

---

## 15. セキュリティ

* トークンをコードに書かない
* Loggerに出力しない
* PropertiesServiceで管理

---

## 16. 関数構成

### code.gs

* syncCalendars
* syncGoogleToOutlook
* syncOutlookToGoogle
* buildGoogleMaps
* buildOutlookMaps
* parseIds
* logAction
* outputSummaryLog

### googleCalendarManager.gs

* getGoogleEvents
* createGoogleEvent
* updateGoogleEvent
* deleteGoogleEvent
* extractGoogleEventId
* buildGoogleDescription

### outlookCalendarManager.gs

* fetchIcs
* parseIcs
* expandRecurringEvents
* createOutlookEvent
* updateOutlookEvent
* deleteOutlookEvent
* getAccessToken
* refreshAccessToken
* buildOutlookDescription

---

以上
